<section>
    {{#if huckster_deck}}
        {{> systems/deadlands_classic/templates/partials/reuseable/deck.hbs deck=huckster_deck deck_type='huckster_deck' hand=huckster_hand}}
    {{/if}}
    <div class="flexrow">
    {{#if tricks}}
    <div>
        <h2 class="center">Tricks</h2>
        {{#each tricks as |hex|}}
        <textarea class="textbox" name="{{this.name}}.description" cols="30" rows="5" wrap="hard">{{this.data.data.description}}</textarea>
        <table>
            <tr class="item" data-itemId="{{this.id}}">
                <td class="perc10"><a class="info-button"><i class="fas fa-info-circle"></i></a></td>
                <td class="perc50">{{this.name}}</td>
                <td class="perc20 center">{{this.data.data.trait}}</td>
                <td class="perc10 center"><a class="sling-trick"><i class="fas fa-bahai" title="Cast {{this.name}}"></i></a></td>
                <td class="perc10 center"><a class="item-delete"><i class="fas fa-trash" title="Remove {{this.name}}"></i></a></td>
            </tr>
        </table>
        {{/each}}
    </div>
    {{/if}}
    {{#if hexes}}
    <div>
        <h2 class="center">Hexes</h2>
        {{#each hexes as |hex|}}
        <textarea class="textbox" name="{{this.name}}.description" cols="30" rows="5" wrap="hard">{{this.data.data.description}}</textarea>
        <table>
            <tr class="item" data-itemId="{{this.id}}">
                <td class="perc10"><a class="info-button"><i class="fas fa-info-circle"></i></a></td>
                <td class="perc40">{{this.name}}</td>
            {{#hex_bounty this.data.data.level}}
                <td class="perc10 right"><a class="magic-buff">[+]</a></td>
            {{/hex_bounty}}
                <td class="perc10">[{{this.data.data.level}}]</td>
                <td class="perc20 center">{{this.data.data.trait}}</td>
                <td class="perc10 center"><a class="sling-hex"><i class="fas fa-bahai" title="Cast {{this.name}}"></i></a></td>
                <td class="perc10 center"><a class="item-delete"><i class="fas fa-trash" title="Remove {{this.name}}"></i></a></td>
            </tr>
        </table>
        {{/each}}
    </div>
    {{/if}}
    </div>
</section>